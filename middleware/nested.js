export default function({ route, redirect }) {
  // if (route.fullPath.startsWith('/cp1/knowledge_type/')) {
  //   console.log('route', route.fullPath)
  //   if (
  //     route.fullPath !== '/cp1/knowledge_type/' &&
  //     route.fullPath.indexOf('knowledge/') < 0
  //   ) {
  //     return redirect(
  //       '/cp1/knowledge_type/' + route.params.type_id + '/knowledge/'
  //     )
  //   }
  // }
  // if (route.fullPath.startsWith('/cp1/orders/')) {
  //   console.log('route', route.params)
  //   if (
  //     route.fullPath !== '/cp1/orders/' &&
  //     route.fullPath.indexOf('order_items') < 0
  //   ) {
  //     return redirect('/cp1/orders/' + route.params.id + '/order_items')
  //   }
  // }
}
